🎯 Guessing App (Java)

A console-based Number Guessing Game built using a Use Case (UC)–driven incremental approach and Git Flow for version control.

The project demonstrates how core Java concepts are applied step by step to build a complete, robust application.
